# Declare global variables to avoid R CMD check NOTES about undefined globals
utils::globalVariables(c(
  ".", "avg_area", "compactness", "density", "geometry",
  "hex_id", "perimeter", "sd", "type"
))
